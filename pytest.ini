[pytest]
asyncio_mode = auto
# Fresh loop per test avoids "Event loop is closed" after DeepEval/langchain work on Windows.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
